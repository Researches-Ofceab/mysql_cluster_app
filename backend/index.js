const express =require('express');

//Create app
const app=express();

app.get('/search:keyword',(res,req)=>{
    
});



app.listen(3000,()=>console.log("The server is running on port : 3000 fir"));
